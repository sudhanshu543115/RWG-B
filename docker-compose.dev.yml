services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.dev

    container_name: rwg-backend

    ports:
      - "5000:5000"

    env_file:
      - .env

    volumes:
      - .:/app
      - /app/node_modules

    restart: unless-stopped